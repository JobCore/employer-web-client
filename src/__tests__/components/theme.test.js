// Requires a store
// import React from "react";
// import { mount } from 'enzyme';
// import Theme from "../../js/components/theme/Theme.jsx";

// describe('<Theme/>', () => {

//     test('Rendering of Theme component', () => {
//         const theme = mount(<Theme onClick={() => this.submithandler()}/>);
//         expect(theme.length).toEqual(1);
//     });

// });